const assert = require('assert');
describe('<name>', function() {
	describe('<method>', function() {
		it('should return <result> when <requirement>', function() {
			assert.equal(1, true);
		});
	});
});
